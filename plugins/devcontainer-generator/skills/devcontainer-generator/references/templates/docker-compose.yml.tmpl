# {{PROJECT_NAME}} Development Container
# Generated by devcontainer-generator v2.3.0
# Note: 'version' key is omitted — Docker Compose v2+ ignores it

services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspaces/{{PROJECT_NAME}}:cached
      # {{CACHE_VOLUMES}} — Insert cache volume mounts from stack reference
    environment:
      # {{ENV_VARS}} — Insert connection strings and env vars from service references
      - NODE_ENV=development
    # {{DEPENDS_ON}} — Insert depends_on from selected services
    cap_add:
      - NET_ADMIN
    command: sleep infinity

  # {{SERVICES}} — Insert service blocks from per-service reference files

volumes:
  # {{NAMED_VOLUMES}} — Insert named volumes from stack and service references
  placeholder:
