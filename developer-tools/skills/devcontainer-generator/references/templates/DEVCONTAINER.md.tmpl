# {{PROJECT_NAME}} — Development Container

> Generated by [devcontainer-generator](https://github.com/MrBogomips/claude-code) v2.4.0

## Configuration Summary

| Setting | Value |
|---------|-------|
| **Stack** | {{STACK_SUMMARY}} |
| **Services** | {{SERVICES_SUMMARY}} |
| **Agentic Tools** | {{TOOLS_SUMMARY}} |
| **Firewall** | {{FIREWALL_POLICY}} |
| **Base Image** | `{{BASE_IMAGE}}` |

## Generated Files

| File | Purpose |
|------|---------|
| `devcontainer.json` | Container features, extensions, ports, lifecycle commands |
| `Dockerfile` | Base image, system dependencies, runtime layers |
| `docker-compose.yml` | Container service + infrastructure services |
| `scripts/post-create.sh` | Git config, tool installation, aliases, package install |
| `scripts/apply-firewall.sh` | Runtime iptables/ip6tables firewall enforcement |
| `firewall-rules.conf` | Domain allowlist/denylist (editable) |

## Services

<!-- {{SERVICES_TABLE}} — Insert service table with ports and credentials -->

| Service | Port(s) | Credentials | Health Check |
|---------|---------|-------------|--------------|

## Host Credential Sharing

<!-- {{CREDENTIAL_DOCS}} — Insert credential sharing documentation if credentials selected in Step 1b -->
<!-- Omitted entirely if no credentials selected -->

The following host credential files are shared with the container via read-only bind mounts:

| Host File | Container Path | Purpose | Fallback Env Var |
|-----------|---------------|---------|-----------------|

**Fallback Chain**: For each credential file, the post-create script uses a 3-level fallback:
1. **Mounted file** — read from the bind-mounted host file (checked with `-s` for non-empty)
2. **Environment variable** — fall back to the listed env var if the file is missing or empty
3. **Warning** — print setup instructions if neither source is available

**Troubleshooting**:
- If `initializeCommand` fails: ensure the host path exists or create it manually
- If extraction finds an empty file: the host file was auto-created as a placeholder — populate it with your actual credentials
- To skip credential sharing entirely: deselect all credential files in Step 1b

## Host Reachability

Dev servers inside the container must bind to `0.0.0.0` (not `localhost` or `127.0.0.1`) to be accessible from the host.

<!-- {{HOST_BINDING}} — Insert per-framework binding guidance -->

| Framework | Command |
|-----------|---------|

## MCP Servers

<!-- {{MCP_SECTION}} — Insert MCP server configuration if any were selected -->

## Customization

### Firewall Rules

Edit `.devcontainer/firewall-rules.conf` to add or remove allowed domains:

```
ALLOW example.com        # Allow specific domain
ALLOW *.example.com      # Allow all subdomains
DENY suspicious.com      # Block specific domain
```

The last rule sets the default policy:
- `DENY *` — block all traffic not explicitly allowed (restrictive)
- `ALLOW *` — allow all traffic (permissive)

Changes take effect on next container start or by running:

```bash
sudo bash /devcontainer/scripts/apply-firewall.sh /devcontainer/firewall-rules.conf
```

### Adding Dependencies

- **System packages**: Edit `Dockerfile` and rebuild
- **Runtime packages**: Edit `scripts/post-create.sh`
- **VS Code extensions**: Edit `devcontainer.json` → `customizations.vscode.extensions`
- **Services**: Add service block to `docker-compose.yml`

## Common Operations

```bash
# Rebuild container (after Dockerfile changes)
# VS Code: Command Palette → "Dev Containers: Rebuild Container"

# Restart container
# VS Code: Command Palette → "Dev Containers: Reopen in Container"

# Reset all volumes (fresh database, caches)
docker compose -f .devcontainer/docker-compose.yml down -v

# Check firewall status
sudo iptables -L DEVCONTAINER_FW -n -v

# Re-apply firewall rules manually
sudo bash /devcontainer/scripts/apply-firewall.sh /devcontainer/firewall-rules.conf
```
