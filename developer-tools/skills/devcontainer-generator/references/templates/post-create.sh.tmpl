#!/bin/bash
# =============================================================================
# {{PROJECT_NAME}} Devcontainer Post-Create Script
# Generated by devcontainer-generator v2.4.0
# =============================================================================

set -e

LOG_PREFIX="[post-create]"
log() { echo "$LOG_PREFIX $1"; }

log "Starting post-create setup..."

# -----------------------------------------------------------------------------
# Git Configuration
# -----------------------------------------------------------------------------
log "Configuring Git..."

git config --global credential.helper store
git config --global core.autocrlf input
git config --global init.defaultBranch main
git config --global color.ui auto
git config --global pull.rebase false
git config --global push.autoSetupRemote true

# {{GIT_LFS}} — Insert if Git LFS selected:
# git lfs install

# -----------------------------------------------------------------------------
# Path Setup
# -----------------------------------------------------------------------------
log "Configuring paths..."

if [[ -d ~/.local ]] && [[ ! -w ~/.local ]]; then
    sudo chown -R "$(id -u):$(id -g)" ~/.local 2>/dev/null || true
fi
mkdir -p ~/.local/bin 2>/dev/null || (sudo mkdir -p ~/.local/bin && sudo chown -R "$(id -u):$(id -g)" ~/.local)

if ! grep -q 'export PATH="$HOME/.local/bin:$PATH"' ~/.bashrc 2>/dev/null; then
    echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
fi
if ! grep -q 'export PATH="$HOME/.local/bin:$PATH"' ~/.zshrc 2>/dev/null; then
    echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc
fi

# {{PATH_EXTRA}} — Insert additional PATH entries from stack reference

# -----------------------------------------------------------------------------
# Host Credential Sharing
# -----------------------------------------------------------------------------
log "Setting up host credentials..."

# {{CREDENTIAL_SETUP}} — Insert credential extraction blocks (3-level fallback each)
# Each block: mounted file → environment variable → warning
# Must run BEFORE {{STACK_SETUP}} so auth tokens are in place for package installs

# -----------------------------------------------------------------------------
# Shell Configuration (Oh My Zsh)
# -----------------------------------------------------------------------------
log "Configuring shell..."

# History settings
cat >> ~/.zshrc << 'ZSHCONFIG'

# History
HISTSIZE=10000
SAVEHIST=10000
setopt HIST_IGNORE_DUPS HIST_IGNORE_SPACE SHARE_HISTORY APPEND_HISTORY INC_APPEND_HISTORY

# Completion
zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z}'

# Directory navigation
setopt AUTO_CD AUTO_PUSHD PUSHD_IGNORE_DUPS PUSHD_SILENT

# FZF integration
[[ -f ~/.fzf.zsh ]] && source ~/.fzf.zsh
export FZF_DEFAULT_OPTS='--height 40% --layout=reverse --border'

# Environment
export EDITOR='code --wait'
export LANG='en_US.UTF-8'
export LC_ALL='en_US.UTF-8'
ZSHCONFIG

# -----------------------------------------------------------------------------
# Agentic Tool Installation
# -----------------------------------------------------------------------------
# {{INSTALL_TOOLS}} — Insert installation blocks from agentic-tools reference files

# -----------------------------------------------------------------------------
# Stack-Specific Setup
# -----------------------------------------------------------------------------
# {{STACK_SETUP}} — Insert post-create steps from stack reference files
# (e.g., dotnet tool install, pnpm install, pip install, etc.)

# -----------------------------------------------------------------------------
# Common Aliases
# -----------------------------------------------------------------------------
log "Setting up aliases..."

ALIASES='
# Navigation
alias ll="ls -la"
alias la="ls -A"
alias l="ls -CF"
alias ..="cd .."
alias ...="cd ../.."

# Project
alias ws="cd /workspaces/{{PROJECT_NAME}}"

# Git
alias gs="git status"
alias gd="git diff"
alias gds="git diff --staged"
alias gl="git log --oneline -20"
alias gp="git pull"
alias gc="git commit"
alias gca="git commit --amend"
alias gb="git branch"
alias gco="git checkout"

# Docker
alias dps="docker ps"
alias dc="docker compose"
alias dcp="docker compose ps"
alias dcl="docker compose logs"
alias dcu="docker compose up -d"
alias dcd="docker compose down"
'

# {{ALIASES_EXTRA}} — Insert stack-specific and tool-specific aliases

echo "$ALIASES" >> ~/.bashrc 2>/dev/null || true
echo "$ALIASES" >> ~/.zshrc 2>/dev/null || true

# -----------------------------------------------------------------------------
# Workspace Setup
# -----------------------------------------------------------------------------
log "Setting up workspace..."

mkdir -p ~/.cache 2>/dev/null || (sudo mkdir -p ~/.cache && sudo chown "$(id -u):$(id -g)" ~/.cache)
mkdir -p ~/.config 2>/dev/null || (sudo mkdir -p ~/.config && sudo chown "$(id -u):$(id -g)" ~/.config)

# -----------------------------------------------------------------------------
# Environment Verification
# -----------------------------------------------------------------------------
log "Verifying environment..."

echo ""
echo "=== {{PROJECT_NAME}} Environment Summary ==="

# {{VERIFY}} — Insert version checks from stack and tool reference files

if command -v docker &> /dev/null; then
    echo "Docker CLI: $(docker --version)"
fi
if command -v git &> /dev/null; then
    echo "Git: $(git --version)"
fi

echo ""
# {{SERVICES_SUMMARY}} — Insert service access info from service reference files
echo "============================================"
echo ""

log "Post-create setup complete!"
